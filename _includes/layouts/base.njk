<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body>

		<header>
			<em>
				{# <a href="{{ '/' | url }}">{{ metadata.logo }}</a> #}
				<a class="logo-container" href="{{ '/' | url }}"><img src="/static/img/hollow-logo.png" class="header-logo" /></a>
			</em>
			{# {% include "components/nav.njk" %} #}
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
